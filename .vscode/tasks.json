{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate",
            "type": "shell",
            "command": "asyncapi generate fromTemplate ../nae-service/api/asyncapi.yaml . --force-write --install -o ./output -p packageName=asyncapigen -p applicationId=nae-service",
            "problemMatcher": []
        }
    ]
}